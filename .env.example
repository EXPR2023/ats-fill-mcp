# ===== ats-fill-mcp 配置（复制为 .env 或直接设置环境变量）=====

# 复用“已登录”的 Chrome：先以远程调试端口启动它，再填此地址
CDP_URL=http://127.0.0.1:9222

# 1 时允许在连不上 CDP 时自行 launch Chromium（未登录场景）
ALLOW_LAUNCH=0

# （可选）语义规划参考简历文件路径；真正语义由 MCP 客户端(Agent)读取并决策
RESUME_PATH=c:\Users\Mr.z\Desktop\release-learn\简历预填个人信息.md
